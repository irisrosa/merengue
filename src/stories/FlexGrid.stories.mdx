import { Meta, Story, Canvas } from '@storybook/addon-docs';

import { data } from './data';
import { FlexGrid } from '../FlexGrid';

<Meta title="FlexGrid/FlexGrid" component={FlexGrid} />

export const Template = (args) => <FlexGrid {...args} />;

<Canvas>
  <p>The FlexGrid component accepts a json representation of the grid with all components.</p>
  <Story
    name="Basic"
    args={{data: data.basic}}>
    {Template.bind({})}
  </Story>
</Canvas>