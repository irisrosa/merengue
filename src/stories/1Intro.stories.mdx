import { Meta } from '@storybook/addon-docs';
import { Block, BlockSet } from '../'

<Meta title="Introduction" />

<h1>Welcome to Flexgrid</h1>

<p>This library can be used to build your grid system in two ways:</p>

<BlockSet>
<Block>
  <p>Directly accessing the `BlockSet` and `Block` components.</p>
</Block>
<Block>
  <p>With the dynamic `FlexGrid` component and a representation of the content in jSON format.</p>
</Block>
</BlockSet>